# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (KEEP SECRET - Server-side only)
# Find this in: Supabase Dashboard → Project Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Admin Setup Key (for creating admin users via API)
# Change this to a random, secure value
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SETUP_KEY=change-me-to-something-random-and-secure

# Resend API Key (for email notifications)
# Get this from: https://resend.com/api-keys
# Free tier: 3,000 emails/month
RESEND_API_KEY=re_YourResendAPIKeyHere
